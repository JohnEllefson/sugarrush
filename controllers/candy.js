'use strict';

const Candy = require('../models/candy.model');

// Retrieve all candy items with optional filtering
const getAllCandy = async (_req, _res) => {};

// Retrieve a single candy item by ID
const getSingleCandy = async (_req, _res) => {};

// Create a new candy item
const createSingleCandy = async (_req, _res) => {};

// Update a single candy item
const updateSingleCandy = async (_req, _res) => {};

// Delete a single candy item
const deleteSingleCandy = async (_req, _res) => {};

module.exports = {
  getAllCandy,
  getSingleCandy,
  createSingleCandy,
  updateSingleCandy,
  deleteSingleCandy
};
